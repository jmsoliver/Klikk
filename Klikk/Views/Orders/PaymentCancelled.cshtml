<section class="payment-cancel-section">

    <div class="container">

        <div class="payment-cancel-card">

            <!-- ICON -->

            <div class="cancel-icon-wrapper">

                <div class="cancel-icon-ring"></div>

                <div class="cancel-icon">

                    <i class="bi bi-x-lg"></i>

                </div>

            </div>

            <!-- CONTENT -->

            <span class="cancel-badge">
                Payment Failed
            </span>

            <h1 class="cancel-title">
                Payment Cancelled
            </h1>

            <p class="cancel-description">
                Your checkout session was cancelled before the payment was completed.
                No charges were made to your account.
            </p>

            <!-- ACTIONS -->

            <div class="cancel-actions">

                <a asp-controller="Cart"
                   asp-action="Index"
                   class="btn klikk-btn-primary cancel-btn">

                    <i class="bi bi-cart"></i>
                    Return To Cart

                </a>

                <a asp-controller="Shop"
                   asp-action="Index"
                   class="btn cancel-secondary-btn">

                    Continue Shopping

                </a>

            </div>

        </div>

    </div>

</section>

<style>

    /* ========================================
           PAYMENT CANCEL PAGE
        ======================================== */

    .payment-cancel-section {
        min-height: 85vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 70px 0;
        background: radial-gradient(circle at top left, rgba(255,77,109,0.08), transparent 28%), radial-gradient(circle at bottom right, rgba(0,0,0,0.05), transparent 25%), #fafafa;
    }

    .payment-cancel-card {
        max-width: 760px;
        margin: auto;
        background: rgba(255,255,255,0.82);
        backdrop-filter: blur(16px);
        border-radius: 34px;
        padding: 70px 55px;
        text-align: center;
        border: 1px solid rgba(255,255,255,0.7);
        box-shadow: 0 25px 70px rgba(0,0,0,0.08);
        position: relative;
        overflow: hidden;
    }

        .payment-cancel-card::before {
            content: "";
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(255,77,109,0.05), transparent 45%);
            pointer-events: none;
        }

    /* ICON */

    .cancel-icon-wrapper {
        position: relative;
        width: 140px;
        height: 140px;
        margin: 0 auto 28px;
    }

    .cancel-icon-ring {
        position: absolute;
        inset: 0;
        border-radius: 50%;
        background: radial-gradient(circle, rgba(255,77,109,0.14), transparent 70%);
        animation: pulseRing 2.4s infinite;
    }

    .cancel-icon {
        position: relative;
        width: 110px;
        height: 110px;
        margin: 15px auto;
        border-radius: 50%;
        background: linear-gradient(135deg, #ff4d6d, #ff758f);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        font-size: 2.7rem;
        box-shadow: 0 18px 45px rgba(255,77,109,0.35);
    }

    @@keyframes pulseRing {

        0% {
            transform: scale(0.92);
            opacity: 0.7;
        }

        70% {
            transform: scale(1.12);
            opacity: 0;
        }

        100% {
            opacity: 0;
        }
    }

    /* TEXT */

    .cancel-badge {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 10px 18px;
        border-radius: 999px;
        background: rgba(255,77,109,0.12);
        color: #ff4d6d;
        font-size: 0.82rem;
        font-weight: 800;
        letter-spacing: 0.5px;
        text-transform: uppercase;
        margin-bottom: 22px;
    }

    .cancel-title {
        font-size: 3rem;
        font-weight: 900;
        color: #111;
        letter-spacing: -1.5px;
        margin-bottom: 18px;
    }

    .cancel-description {
        max-width: 560px;
        margin: auto;
        color: #666;
        font-size: 1.08rem;
        line-height: 1.9;
    }

    /* BUTTONS */

    .cancel-actions {
        margin-top: 38px;
        display: flex;
        justify-content: center;
        gap: 14px;
        flex-wrap: wrap;
    }

    .cancel-btn,
    .cancel-secondary-btn {
        height: 56px;
        min-width: 210px;
        border-radius: 16px;
        font-weight: 700;
        font-size: 0.95rem;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        transition: all 0.25s ease;
        padding: 0 24px;
    }

        .cancel-btn:hover,
        .cancel-secondary-btn:hover {
            transform: translateY(-3px);
        }

    .cancel-secondary-btn {
        background: #fff;
        border: 1px solid rgba(0,0,0,0.08);
        color: #111;
        text-decoration: none;
        box-shadow: 0 8px 22px rgba(0,0,0,0.05);
    }

        .cancel-secondary-btn:hover {
            color: #111;
            background: #f8f8f8;
        }

    /* MOBILE */

    @@media (max-width: 768px) {

        .payment-cancel-card {
            padding: 50px 28px;
            border-radius: 28px;
        }

        .cancel-title {
            font-size: 2.3rem;
        }

        .cancel-actions {
            flex-direction: column;
        }

        .cancel-btn,
        .cancel-secondary-btn {
            width: 100%;
        }
    }

</style>