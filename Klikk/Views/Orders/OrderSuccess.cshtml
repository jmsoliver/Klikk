<section class="payment-success-section">

    <div class="container">

        <div class="payment-success-card">

            <!-- ICON -->

            <div class="success-icon-wrapper">

                <div class="success-icon-ring"></div>

                <div class="success-icon">

                    <i class="bi bi-bag-check-fill"></i>

                </div>

            </div>

            <!-- CONTENT -->

            <span class="success-badge">
                Order Confirmed
            </span>

            <h1 class="success-title">
                Order Placed Successfully!
            </h1>

            <p class="success-description">
                Thank you for shopping with Klikk.
                Your order has been received successfully and is now being processed by our team.
            </p>

            <!-- ACTIONS -->

            <div class="success-actions">

                <a asp-controller="Orders"
                   asp-action="MyOrders"
                   class="btn klikk-btn-primary success-btn">

                    <i class="bi bi-box-seam"></i>
                    Track My Orders

                </a>

                <a asp-controller="Products"
                   asp-action="Index"
                   class="btn success-secondary-btn">

                    Continue Shopping

                </a>

            </div>

        </div>

    </div>

</section>

<style>

    /* ========================================
           ORDER SUCCESS PAGE
        ======================================== */

    .payment-success-section {
        min-height: 85vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 70px 0;
        background: radial-gradient(circle at top left, rgba(25,135,84,0.08), transparent 28%), radial-gradient(circle at bottom right, rgba(0,0,0,0.05), transparent 25%), #fafafa;
    }

    .payment-success-card {
        max-width: 760px;
        margin: auto;
        background: rgba(255,255,255,0.82);
        backdrop-filter: blur(16px);
        border-radius: 34px;
        padding: 70px 55px;
        text-align: center;
        border: 1px solid rgba(255,255,255,0.7);
        box-shadow: 0 25px 70px rgba(0,0,0,0.08);
        position: relative;
        overflow: hidden;
    }

        .payment-success-card::before {
            content: "";
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, rgba(25,135,84,0.05), transparent 45%);
            pointer-events: none;
        }

    /* ICON */

    .success-icon-wrapper {
        position: relative;
        width: 140px;
        height: 140px;
        margin: 0 auto 28px;
    }

    .success-icon-ring {
        position: absolute;
        inset: 0;
        border-radius: 50%;
        background: radial-gradient(circle, rgba(25,135,84,0.14), transparent 70%);
        animation: pulseSuccessRing 2.4s infinite;
    }

    .success-icon {
        position: relative;
        width: 110px;
        height: 110px;
        margin: 15px auto;
        border-radius: 50%;
        background: linear-gradient(135deg, #198754, #33c27f);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        font-size: 2.8rem;
        box-shadow: 0 18px 45px rgba(25,135,84,0.35);
    }

    @@keyframes pulseSuccessRing {

        0% {
            transform: scale(0.92);
            opacity: 0.7;
        }

        70% {
            transform: scale(1.12);
            opacity: 0;
        }

        100% {
            opacity: 0;
        }
    }

    /* TEXT */

    .success-badge {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 10px 18px;
        border-radius: 999px;
        background: rgba(25,135,84,0.12);
        color: #198754;
        font-size: 0.82rem;
        font-weight: 800;
        letter-spacing: 0.5px;
        text-transform: uppercase;
        margin-bottom: 22px;
    }

    .success-title {
        font-size: 3rem;
        font-weight: 900;
        color: #111;
        letter-spacing: -1.5px;
        margin-bottom: 18px;
    }

    .success-description {
        max-width: 560px;
        margin: auto;
        color: #666;
        font-size: 1.08rem;
        line-height: 1.9;
    }

    /* BUTTONS */

    .success-actions {
        margin-top: 38px;
        display: flex;
        justify-content: center;
        gap: 14px;
        flex-wrap: wrap;
    }

    .success-btn,
    .success-secondary-btn {
        height: 56px;
        min-width: 220px;
        border-radius: 16px;
        font-weight: 700;
        font-size: 0.95rem;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        transition: all 0.25s ease;
        padding: 0 24px;
    }

        .success-btn:hover,
        .success-secondary-btn:hover {
            transform: translateY(-3px);
        }

    .success-secondary-btn {
        background: #fff;
        border: 1px solid rgba(0,0,0,0.08);
        color: #111;
        text-decoration: none;
        box-shadow: 0 8px 22px rgba(0,0,0,0.05);
    }

        .success-secondary-btn:hover {
            color: #111;
            background: #f8f8f8;
        }

    /* MOBILE */

    @@media (max-width: 768px) {

        .payment-success-card {
            padding: 50px 28px;
            border-radius: 28px;
        }

        .success-title {
            font-size: 2.3rem;
        }

        .success-actions {
            flex-direction: column;
        }

        .success-btn,
        .success-secondary-btn {
            width: 100%;
        }
    }

</style>